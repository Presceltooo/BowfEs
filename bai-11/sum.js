export const sum = (a,b) => {
  return a + b;
};

// đôi khi chúng ta cần đổi tên module vì 1 lí do nào đó, thì ta có 2 cách đổi ở export hoặc đổi ở import(thấy trùng mới bắt đầu đi đổi)

// export { sum as tinhTong }; 