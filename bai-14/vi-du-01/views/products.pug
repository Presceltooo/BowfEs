extends layouts/default.pug
include mixins/box-head.pug

block main
  // sử dụng mixins
  +box-head("Trang danh sách sản phẩm", "Đây là đoan mô tả của Trang danh sách sản phẩm")

  ul
    each item in products 
      li #{item.title} - #{item.price} - #{item.description}
      //- đã học đc 1h21p
